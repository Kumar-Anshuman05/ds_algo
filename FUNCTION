Q. FIND THE FACTORIAL OF A GIVEN NUMBER USING FUNCTION ONLY(NO FOR , WHILE LOOP)IN C++

#include<iostream>
using namespace std ;
int factorial(int n)
{
    
    if(n>1)
    {
    return n*factorial(n-1);
    }
    return(n);
}

int main()
{
    int n=7;
    cout<<"factorial of "<<n<<" ="<<factorial(n);
}
