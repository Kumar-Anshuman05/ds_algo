#include<iostream>
using namespace std ;
int factorial(int n)
{
    
    if(n>1)
    {
    return n*factorial(n-1);
    }
    return(n);
}

int main()
{
    int n=7;
    cout<<"factorial of "<<n<<" ="<<factorial(n);
}
